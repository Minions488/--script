--[[
    巫script | 离家同盟 · 自动刷金币（自杀重播 + UI + 自动覆盖更新版）
--]]

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local LocalPlayer = Players.LocalPlayer
local SERVER_ENTRY_TIME = tick()

local SCRIPT_FILE = "Runaways_AutoGold_Fixed.lua"

local isRunning = false
local isVictoryRunning = false
local RemoteEvent = nil

----------------------------------------------------------------
-- 完整源代码 (用于更新本地缓存，包含 UI)
----------------------------------------------------------------
local CURRENT_SCRIPT_SOURCE = [==[
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local LocalPlayer = Players.LocalPlayer
local SERVER_ENTRY_TIME = tick()

local SCRIPT_FILE = "Runaways_AutoGold_Fixed.lua"

local isRunning = false
local isVictoryRunning = false
local RemoteEvent = nil

local function getQueueFn()
    local list = {
        queue_on_teleport,
        syn and syn.queue_on_teleport,
        fluxus and fluxus.queue_on_teleport,
        Krnl and Krnl.queue_on_teleport,
        getgenv and getgenv().queue_on_teleport,
    }
    for _, fn in ipairs(list) do
        if typeof(fn) == "function" then return fn end
    end
    return nil
end

local function queueReload()
    local q = getQueueFn()
    if not q then return false end
    local payload = 'repeat task.wait() until game:IsLoaded() if isfile and isfile("' .. SCRIPT_FILE .. '") then loadstring(readfile("' .. SCRIPT_FILE .. '"))() end'
    pcall(q, payload)
end

local function bindRemote()
    if RemoteEvent then return true end
    pcall(function()
        local fc = ReplicatedStorage:FindFirstChild("FlowClient") or ReplicatedStorage:WaitForChild("FlowClient", 12)
        local runner = fc and (fc:FindFirstChild("ClientRunner") or fc:WaitForChild("ClientRunner", 8))
        RemoteEvent = runner and (runner:FindFirstChild("Event") or runner:WaitForChild("Event", 8))
    end)
    return RemoteEvent ~= nil
end

local function fireReplay()
    if not RemoteEvent then bindRemote() end
    if RemoteEvent then
        pcall(function() RemoteEvent:FireServer("GameManager", "Replay") end)
    end
end

local function runVictory()
    if isVictoryRunning then return end
    isVictoryRunning = true

    task.spawn(function()
        pcall(function()
            local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local hrp = character:WaitForChild("HumanoidRootPart", 10)
            local humanoid = character:WaitForChild("Humanoid", 10)
            if not hrp or not humanoid then return end

            local noclipConn = RunService.Stepped:Connect(function()
                if character then
                    for _, part in ipairs(character:GetDescendants()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end
            end)

            local function tp(cf)
                hrp.Anchored = true
                character:PivotTo(cf)
                task.wait(0.5)
                hrp.Anchored = false
            end

            tp(CFrame.new(1780, 1940, 83615) * CFrame.new(0, 5, 0))

            local map = workspace:FindFirstChild("Map")
            local buildings = map and map:FindFirstChild("Buildings")
            local customsFinal = buildings and buildings:FindFirstChild("CustomsFinal")

            if customsFinal then
                local pivot = customsFinal:IsA("Model") and customsFinal:GetPivot() or customsFinal.CFrame
                tp(pivot * CFrame.new(0, 5, 20))
            end

            -- 从进入服务器/脚本启动开始计时，确保自动死亡发生在约 6 秒后
            local remaining = 6 - (tick() - SERVER_ENTRY_TIME)
            if remaining > 0 then
                task.wait(remaining)
            end

            if humanoid and humanoid.Health > 0 then
                humanoid.Health = 0
            else
                character:BreakJoints()
            end

            task.wait(1)

            local t0 = tick()
            while isRunning and isVictoryRunning and tick() - t0 < 30 do
                fireReplay()
                task.wait(0.8)
            end

            if noclipConn then noclipConn:Disconnect() end
        end)
        isVictoryRunning = false
    end)
end

local function findReplayBtn()
    local pg = LocalPlayer:FindFirstChild("PlayerGui")
    if not pg then return nil end
    local endFrame = pg:FindFirstChild("EndFrame")
    if endFrame then
        local f = endFrame:FindFirstChild("Frame")
        local opt = f and f:FindFirstChild("Options")
        local r = opt and opt:FindFirstChild("Replay")
        if r then return r end
    end
    for _, d in ipairs(pg:GetDescendants()) do
        if d.Name == "Replay" and d:IsA("GuiButton") then return d end
    end
    return nil
end

local function startFarm()
    if isRunning then return end
    isRunning = true
    queueReload()
    bindRemote()

    task.spawn(function()
        while isRunning do
            queueReload()
            if not isVictoryRunning then runVictory() end
            local t0 = tick()
            while isRunning and isVictoryRunning and tick() - t0 < 30 do task.wait(1) end
            fireReplay()
            task.wait(2)
        end
    end)

    task.spawn(function()
        while isRunning do
            local btn = findReplayBtn()
            if btn and btn.Visible and btn.AbsoluteSize.X > 0 then
                fireReplay()
                task.wait(1.5)
            end
            task.wait(0.5)
        end
    end)
end

local function stopFarm()
    isRunning = false
end

-- UI Component
local function getUiParent()
    local ok, h = pcall(function() if gethui then return gethui() end end)
    if ok and h then return h end
    ok, h = pcall(function() return game:GetService("CoreGui") end)
    if ok and h then return h end
    return LocalPlayer:WaitForChild("PlayerGui")
end

pcall(function()
    local parent = getUiParent()
    local old = parent:FindFirstChild("RunawaysAutoGoldUI")
    if old then old:Destroy() end
end)

local gui = Instance.new("ScreenGui")
gui.Name = "RunawaysAutoGoldUI"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.DisplayOrder = 9999
gui.Parent = getUiParent()

local frame = Instance.new("Frame")
frame.Size = UDim2.fromOffset(280, 130)
frame.Position = UDim2.new(0.5, -140, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(28, 28, 36)
frame.Active = true
frame.Draggable = true
frame.Parent = gui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -16, 0, 28)
title.Position = UDim2.fromOffset(8, 8)
title.BackgroundTransparency = 1
title.Text = "巫script | 离家同盟 · 刷金币"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 15
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

local sub = Instance.new("TextLabel")
sub.Size = UDim2.new(1, -16, 0, 18)
sub.Position = UDim2.fromOffset(8, 36)
sub.BackgroundTransparency = 1
sub.Text = "状态：已自动运行中"
sub.TextColor3 = Color3.fromRGB(160, 160, 170)
sub.Font = Enum.Font.Gotham
sub.TextSize = 12
sub.TextXAlignment = Enum.TextXAlignment.Left
sub.Parent = frame

local btn = Instance.new("TextButton")
btn.Size = UDim2.new(1, -24, 0, 44)
btn.Position = UDim2.fromOffset(12, 64)
btn.BackgroundColor3 = Color3.fromRGB(160, 90, 30)
btn.Text = "关闭自动刷金币"
btn.TextColor3 = Color3.fromRGB(255, 255, 255)
btn.Font = Enum.Font.GothamBold
btn.TextSize = 16
btn.Parent = frame
Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

local function refreshBtn()
    if isRunning then
        btn.Text = "关闭自动刷金币"
        btn.BackgroundColor3 = Color3.fromRGB(160, 90, 30)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        sub.Text = "状态：运行中..."
    else
        btn.Text = "开启自动刷金币"
        btn.BackgroundColor3 = Color3.fromRGB(70, 55, 30)
        btn.TextColor3 = Color3.fromRGB(255, 230, 180)
        sub.Text = "状态：已停止"
    end
end

btn.MouseButton1Click:Connect(function()
    if isRunning then stopFarm() else startFarm() end
    refreshBtn()
end)

task.spawn(function()
    while task.wait(2) do
        if gui and not gui.Parent then gui.Parent = getUiParent() end
    end
end)

task.spawn(function()
    task.wait(1)
    bindRemote()
    queueReload()
    startFarm()
    refreshBtn()
end)
]==]

----------------------------------------------------------------
-- 强制写入/覆盖本地缓存文件
----------------------------------------------------------------
local function updateLocalCache()
    pcall(function()
        if writefile then
            writefile(SCRIPT_FILE, CURRENT_SCRIPT_SOURCE)
            print("[巫script] 本地代码已覆盖更新:", SCRIPT_FILE)
        end
    end)
end

----------------------------------------------------------------
-- 跳服重载
----------------------------------------------------------------
local function getQueueFn()
    local list = {
        queue_on_teleport,
        syn and syn.queue_on_teleport,
        fluxus and fluxus.queue_on_teleport,
        Krnl and Krnl.queue_on_teleport,
        getgenv and getgenv().queue_on_teleport,
    }
    for _, fn in ipairs(list) do
        if typeof(fn) == "function" then return fn end
    end
    return nil
end

local function queueReload()
    local q = getQueueFn()
    if not q then return false end

    updateLocalCache()

    local payload = 'repeat task.wait() until game:IsLoaded() if isfile and isfile("' .. SCRIPT_FILE .. '") then loadstring(readfile("' .. SCRIPT_FILE .. '"))() end'

    local ok, err = pcall(q, payload)
    if ok then
        print("[巫script] 已登记跳服自动重载")
    else
        warn("[巫script] queue 失败:", err)
    end
    return ok
end

----------------------------------------------------------------
-- 远程 API
----------------------------------------------------------------
local function bindRemote()
    if RemoteEvent then return true end
    local ok = pcall(function()
        local fc = ReplicatedStorage:FindFirstChild("FlowClient")
            or ReplicatedStorage:WaitForChild("FlowClient", 12)
        local runner = fc and (fc:FindFirstChild("ClientRunner") or fc:WaitForChild("ClientRunner", 8))
        RemoteEvent = runner and (runner:FindFirstChild("Event") or runner:WaitForChild("Event", 8))
    end)
    return ok and RemoteEvent ~= nil
end

local function fireReplay()
    if not RemoteEvent then bindRemote() end
    if RemoteEvent then
        pcall(function()
            RemoteEvent:FireServer("GameManager", "Replay")
        end)
    end
end

----------------------------------------------------------------
-- 传送终点与自杀
----------------------------------------------------------------
local function runVictory()
    if isVictoryRunning then return end
    isVictoryRunning = true

    task.spawn(function()
        local ok, err = pcall(function()
            local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local hrp = character:WaitForChild("HumanoidRootPart", 10)
            local humanoid = character:WaitForChild("Humanoid", 10)
            if not hrp or not humanoid then return end

            local noclipConn = RunService.Stepped:Connect(function()
                if character then
                    for _, part in ipairs(character:GetDescendants()) do
                        if part:IsA("BasePart") then
                            part.CanCollide = false
                        end
                    end
                end
            end)

            local function tp(cf)
                hrp.Anchored = true
                character:PivotTo(cf)
                task.wait(0.5)
                hrp.Anchored = false
            end

            tp(CFrame.new(1780, 1940, 83615) * CFrame.new(0, 5, 0))

            local map = workspace:FindFirstChild("Map")
            local buildings = map and map:FindFirstChild("Buildings")
            local customsFinal = buildings and buildings:FindFirstChild("CustomsFinal")

            if customsFinal then
                local pivot = customsFinal:IsA("Model") and customsFinal:GetPivot() or customsFinal.CFrame
                tp(pivot * CFrame.new(0, 5, 20))
            end

            -- 从进入服务器/脚本启动开始计时，确保自动死亡发生在约 3 秒后
            local remaining = 3 - (tick() - SERVER_ENTRY_TIME)
            if remaining > 0 then
                task.wait(remaining)
            end

            if humanoid and humanoid.Health > 0 then
                humanoid.Health = 0
            else
                character:BreakJoints()
            end

            task.wait(1)

            local t0 = tick()
            while isRunning and isVictoryRunning and tick() - t0 < 30 do
                fireReplay()
                task.wait(0.8)
            end

            if noclipConn then noclipConn:Disconnect() end
        end)
        if not ok then warn("[巫script] 流程出错:", err) end
        isVictoryRunning = false
    end)
end

----------------------------------------------------------------
-- 检测 Replay 按钮
----------------------------------------------------------------
local function findReplayBtn()
    local pg = LocalPlayer:FindFirstChild("PlayerGui")
    if not pg then return nil end
    local endFrame = pg:FindFirstChild("EndFrame")
    if endFrame then
        local f = endFrame:FindFirstChild("Frame")
        local opt = f and f:FindFirstChild("Options")
        local r = opt and opt:FindFirstChild("Replay")
        if r then return r end
    end
    for _, d in ipairs(pg:GetDescendants()) do
        if d.Name == "Replay" and d:IsA("GuiButton") then
            return d
        end
    end
    return nil
end

----------------------------------------------------------------
-- 启动控制
----------------------------------------------------------------
local function startFarm()
    if isRunning then return end
    isRunning = true
    queueReload()
    bindRemote()

    task.spawn(function()
        while isRunning do
            queueReload()
            if not isVictoryRunning then
                runVictory()
            end
            local t0 = tick()
            while isRunning and isVictoryRunning and tick() - t0 < 30 do
                task.wait(1)
            end
            fireReplay()
            task.wait(2)
        end
    end)

    task.spawn(function()
        while isRunning do
            local btn = findReplayBtn()
            if btn and btn.Visible and btn.AbsoluteSize.X > 0 then
                fireReplay()
                task.wait(1.5)
            end
            task.wait(0.5)
        end
    end)

    print("[巫script] 已开启：终点自杀 + UI 控制")
end

local function stopFarm()
    isRunning = false
    print("[巫script] 已停止")
end

----------------------------------------------------------------
-- UI 绘制
----------------------------------------------------------------
local function getUiParent()
    local ok, h = pcall(function()
        if gethui then return gethui() end
    end)
    if ok and h then return h end
    ok, h = pcall(function() return game:GetService("CoreGui") end)
    if ok and h then return h end
    return LocalPlayer:WaitForChild("PlayerGui")
end

pcall(function()
    local parent = getUiParent()
    local old = parent:FindFirstChild("RunawaysAutoGoldUI")
    if old then old:Destroy() end
end)

local gui = Instance.new("ScreenGui")
gui.Name = "RunawaysAutoGoldUI"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.DisplayOrder = 9999
gui.Parent = getUiParent()

local frame = Instance.new("Frame")
frame.Size = UDim2.fromOffset(280, 130)
frame.Position = UDim2.new(0.5, -140, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(28, 28, 36)
frame.Active = true
frame.Draggable = true
frame.Parent = gui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -16, 0, 28)
title.Position = UDim2.fromOffset(8, 8)
title.BackgroundTransparency = 1
title.Text = "巫script | 离家同盟 · 刷金币"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 15
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

local sub = Instance.new("TextLabel")
sub.Size = UDim2.new(1, -16, 0, 18)
sub.Position = UDim2.fromOffset(8, 36)
sub.BackgroundTransparency = 1
sub.Text = "状态：已自动运行中"
sub.TextColor3 = Color3.fromRGB(160, 160, 170)
sub.Font = Enum.Font.Gotham
sub.TextSize = 12
sub.TextXAlignment = Enum.TextXAlignment.Left
sub.Parent = frame

local btn = Instance.new("TextButton")
btn.Size = UDim2.new(1, -24, 0, 44)
btn.Position = UDim2.fromOffset(12, 64)
btn.BackgroundColor3 = Color3.fromRGB(160, 90, 30)
btn.Text = "关闭自动刷金币"
btn.TextColor3 = Color3.fromRGB(255, 255, 255)
btn.Font = Enum.Font.GothamBold
btn.TextSize = 16
btn.Parent = frame
Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

local function refreshBtn()
    if isRunning then
        btn.Text = "关闭自动刷金币"
        btn.BackgroundColor3 = Color3.fromRGB(160, 90, 30)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        sub.Text = "状态：运行中..."
    else
        btn.Text = "开启自动刷金币"
        btn.BackgroundColor3 = Color3.fromRGB(70, 55, 30)
        btn.TextColor3 = Color3.fromRGB(255, 230, 180)
        sub.Text = "状态：已停止"
    end
end

btn.MouseButton1Click:Connect(function()
    if isRunning then
        stopFarm()
    else
        startFarm()
    end
    refreshBtn()
end)

task.spawn(function()
    while task.wait(2) do
        if gui and not gui.Parent then
            gui.Parent = getUiParent()
        end
    end
end)

----------------------------------------------------------------
-- 执行
----------------------------------------------------------------
task.spawn(function()
    task.wait(1)
    updateLocalCache()
    bindRemote()
    queueReload()
    startFarm()
    refreshBtn()
end)
