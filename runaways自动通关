--[[
    巫script | 离家同盟 · 自动刷金币
    - 约 6 秒传送终点，约 7 秒自杀
    - 关闭后写入关闭标记，跳服不再自动加载
]]

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer
local SERVER_ENTRY_TIME = tick()

local SCRIPT_FILE = "Runaways_AutoGold_Fixed.lua"
local OFF_FILE = "Runaways_AutoGold_OFF.txt" -- 存在 = 用户已关闭，禁止自动加载

local isRunning = false
local isVictoryRunning = false
local RemoteEvent = nil

----------------------------------------------------------------
-- 关闭标记（解决：关掉后跳服还自动加载）
----------------------------------------------------------------
local function isUserDisabled()
    local off = false
    pcall(function()
        if isfile and isfile(OFF_FILE) then
            local c = tostring(readfile(OFF_FILE) or "")
            if c == "1" or c:lower():find("off") or c == "true" then
                off = true
            end
        end
    end)
    return off
end

local function setUserDisabled(disabled)
    pcall(function()
        if not writefile then return end
        if disabled then
            writefile(OFF_FILE, "1")
        else
            if isfile and isfile(OFF_FILE) and delfile then
                pcall(delfile, OFF_FILE)
            else
                writefile(OFF_FILE, "0")
            end
        end
    end)
end

----------------------------------------------------------------
-- 内嵌源码（跳服用，与下方逻辑一致）
----------------------------------------------------------------
local CURRENT_SCRIPT_SOURCE = [==[
local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local LocalPlayer = Players.LocalPlayer
local SERVER_ENTRY_TIME = tick()

local SCRIPT_FILE = "Runaways_AutoGold_Fixed.lua"
local OFF_FILE = "Runaways_AutoGold_OFF.txt"

local isRunning = false
local isVictoryRunning = false
local RemoteEvent = nil

local function isUserDisabled()
    local off = false
    pcall(function()
        if isfile and isfile(OFF_FILE) then
            local c = tostring(readfile(OFF_FILE) or "")
            if c == "1" or c:lower():find("off") or c == "true" then off = true end
        end
    end)
    return off
end

local function setUserDisabled(disabled)
    pcall(function()
        if not writefile then return end
        if disabled then
            writefile(OFF_FILE, "1")
        else
            if isfile and isfile(OFF_FILE) and delfile then pcall(delfile, OFF_FILE)
            else writefile(OFF_FILE, "0") end
        end
    end)
end

-- 若用户已关闭，跳服加载后直接退出（不跑农场、不弹强制 UI 循环）
if isUserDisabled() then
    print("[巫script] 用户已关闭自动刷，跳过")
    return
end

local function getQueueFn()
    local list = {
        queue_on_teleport,
        syn and syn.queue_on_teleport,
        fluxus and fluxus.queue_on_teleport,
        Krnl and Krnl.queue_on_teleport,
        getgenv and getgenv().queue_on_teleport,
    }
    for _, fn in ipairs(list) do
        if typeof(fn) == "function" then return fn end
    end
    return nil
end

local function queueReload()
    if isUserDisabled() or not isRunning then return false end
    local q = getQueueFn()
    if not q then return false end
    local payload = [[
repeat task.wait() until game:IsLoaded()
pcall(function()
    if isfile and isfile("Runaways_AutoGold_OFF.txt") then
        local c = tostring(readfile("Runaways_AutoGold_OFF.txt") or "")
        if c == "1" or c:lower():find("off") or c == "true" then return end
    end
    if isfile and isfile("Runaways_AutoGold_Fixed.lua") then
        loadstring(readfile("Runaways_AutoGold_Fixed.lua"))()
    end
end)
]]
    pcall(q, payload)
    return true
end

local function bindRemote()
    if RemoteEvent then return true end
    pcall(function()
        local fc = ReplicatedStorage:FindFirstChild("FlowClient") or ReplicatedStorage:WaitForChild("FlowClient", 12)
        local runner = fc and (fc:FindFirstChild("ClientRunner") or fc:WaitForChild("ClientRunner", 8))
        RemoteEvent = runner and (runner:FindFirstChild("Event") or runner:WaitForChild("Event", 8))
    end)
    return RemoteEvent ~= nil
end

local function fireReplay()
    if not RemoteEvent then bindRemote() end
    if RemoteEvent then
        pcall(function() RemoteEvent:FireServer("GameManager", "Replay") end)
    end
end

local function runVictory()
    if isVictoryRunning or not isRunning then return end
    isVictoryRunning = true
    task.spawn(function()
        pcall(function()
            local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local hrp = character:WaitForChild("HumanoidRootPart", 10)
            local humanoid = character:WaitForChild("Humanoid", 10)
            if not hrp or not humanoid then return end

            local function tp(cf)
                if not isRunning then return end
                hrp.Anchored = true
                character:PivotTo(cf)
                task.wait(0.35)
                hrp.Anchored = false
            end

            -- 6 秒后传送
            local waitTp = 6 - (tick() - SERVER_ENTRY_TIME)
            if waitTp > 0 then task.wait(waitTp) end
            if not isRunning then return end

            tp(CFrame.new(1780, 1940, 83615) * CFrame.new(0, 5, 0))
            if not isRunning then return end

            local map = workspace:FindFirstChild("Map")
            local buildings = map and map:FindFirstChild("Buildings")
            local customsFinal = buildings and buildings:FindFirstChild("CustomsFinal")
            if customsFinal then
                local pivot = customsFinal:IsA("Model") and customsFinal:GetPivot() or customsFinal.CFrame
                tp(pivot * CFrame.new(0, 5, 20))
            end
            if not isRunning then return end

            -- 7 秒后自杀
            local waitDie = 7 - (tick() - SERVER_ENTRY_TIME)
            if waitDie > 0 then task.wait(waitDie) end
            if not isRunning then return end

            if humanoid and humanoid.Health > 0 then
                humanoid.Health = 0
            else
                pcall(function() character:BreakJoints() end)
            end

            task.wait(1)
            local t0 = tick()
            while isRunning and isVictoryRunning and tick() - t0 < 25 do
                fireReplay()
                task.wait(0.8)
            end
        end)
        isVictoryRunning = false
    end)
end

local function findReplayBtn()
    local pg = LocalPlayer:FindFirstChild("PlayerGui")
    if not pg then return nil end
    local endFrame = pg:FindFirstChild("EndFrame")
    if endFrame then
        local f = endFrame:FindFirstChild("Frame")
        local opt = f and f:FindFirstChild("Options")
        local r = opt and opt:FindFirstChild("Replay")
        if r then return r end
    end
    for _, d in ipairs(pg:GetDescendants()) do
        if d.Name == "Replay" and d:IsA("GuiButton") then return d end
    end
    return nil
end

local function startFarm()
    if isRunning then return end
    setUserDisabled(false)
    isRunning = true
    queueReload()
    bindRemote()

    task.spawn(function()
        while isRunning do
            if isUserDisabled() then break end
            queueReload()
            if not isVictoryRunning then runVictory() end
            local t0 = tick()
            while isRunning and isVictoryRunning and tick() - t0 < 30 do task.wait(0.5) end
            if isRunning then fireReplay() end
            task.wait(1.5)
        end
    end)

    task.spawn(function()
        while isRunning do
            local btn = findReplayBtn()
            if btn and btn.Visible and btn.AbsoluteSize.X > 0 then
                fireReplay()
                task.wait(1.2)
            end
            task.wait(0.4)
        end
    end)
end

local function stopFarm()
    isRunning = false
    isVictoryRunning = false
    setUserDisabled(true)
    print("[巫script] 已关闭，并禁止跳服自动加载")
end

local function getUiParent()
    local ok, h = pcall(function() if gethui then return gethui() end end)
    if ok and h then return h end
    ok, h = pcall(function() return game:GetService("CoreGui") end)
    if ok and h then return h end
    return LocalPlayer:WaitForChild("PlayerGui")
end

pcall(function()
    local parent = getUiParent()
    local old = parent:FindFirstChild("RunawaysAutoGoldUI")
    if old then old:Destroy() end
end)

local gui = Instance.new("ScreenGui")
gui.Name = "RunawaysAutoGoldUI"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.DisplayOrder = 9999
gui.Parent = getUiParent()

local frame = Instance.new("Frame")
frame.Size = UDim2.fromOffset(280, 130)
frame.Position = UDim2.new(0.5, -140, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(28, 28, 36)
frame.Active = true
frame.Draggable = true
frame.Parent = gui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -16, 0, 28)
title.Position = UDim2.fromOffset(8, 8)
title.BackgroundTransparency = 1
title.Text = "巫script | 离家同盟 · 刷金币"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 15
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

local sub = Instance.new("TextLabel")
sub.Size = UDim2.new(1, -16, 0, 18)
sub.Position = UDim2.fromOffset(8, 36)
sub.BackgroundTransparency = 1
sub.Text = "状态：—"
sub.TextColor3 = Color3.fromRGB(160, 160, 170)
sub.Font = Enum.Font.Gotham
sub.TextSize = 12
sub.TextXAlignment = Enum.TextXAlignment.Left
sub.Parent = frame

local btn = Instance.new("TextButton")
btn.Size = UDim2.new(1, -24, 0, 44)
btn.Position = UDim2.fromOffset(12, 64)
btn.BackgroundColor3 = Color3.fromRGB(70, 55, 30)
btn.Text = "开启自动刷金币"
btn.TextColor3 = Color3.fromRGB(255, 230, 180)
btn.Font = Enum.Font.GothamBold
btn.TextSize = 16
btn.Parent = frame
Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

local function refreshBtn()
    if isRunning then
        btn.Text = "关闭自动刷金币"
        btn.BackgroundColor3 = Color3.fromRGB(160, 90, 30)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        sub.Text = "状态：运行中（6s传送 / 7s自杀）"
    else
        btn.Text = "开启自动刷金币"
        btn.BackgroundColor3 = Color3.fromRGB(70, 55, 30)
        btn.TextColor3 = Color3.fromRGB(255, 230, 180)
        sub.Text = "状态：已停止（不会自动加载）"
    end
end

btn.MouseButton1Click:Connect(function()
    if isRunning then
        stopFarm()
    else
        SERVER_ENTRY_TIME = tick()
        startFarm()
    end
    refreshBtn()
end)

task.spawn(function()
    while task.wait(2) do
        if gui and not gui.Parent then gui.Parent = getUiParent() end
    end
end)

-- 仅在未关闭时自动开刷
task.spawn(function()
    task.wait(0.8)
    bindRemote()
    if isUserDisabled() then
        refreshBtn()
        print("[巫script] 检测到关闭标记，不自动开刷")
        return
    end
    startFarm()
    refreshBtn()
end)
]==]

----------------------------------------------------------------
-- 外层：同逻辑（首次执行用）
----------------------------------------------------------------
local function getQueueFn()
    local list = {
        queue_on_teleport,
        syn and syn.queue_on_teleport,
        fluxus and fluxus.queue_on_teleport,
        Krnl and Krnl.queue_on_teleport,
        getgenv and getgenv().queue_on_teleport,
    }
    for _, fn in ipairs(list) do
        if typeof(fn) == "function" then return fn end
    end
    return nil
end

local function updateLocalCache()
    pcall(function()
        if writefile then
            writefile(SCRIPT_FILE, CURRENT_SCRIPT_SOURCE)
            print("[巫script] 已更新本地缓存", SCRIPT_FILE)
        end
    end)
end

local function queueReload()
    if isUserDisabled() or not isRunning then return false end
    local q = getQueueFn()
    if not q then return false end
    updateLocalCache()
    local payload = [[
repeat task.wait() until game:IsLoaded()
pcall(function()
    if isfile and isfile("Runaways_AutoGold_OFF.txt") then
        local c = tostring(readfile("Runaways_AutoGold_OFF.txt") or "")
        if c == "1" or c:lower():find("off") or c == "true" then return end
    end
    if isfile and isfile("Runaways_AutoGold_Fixed.lua") then
        loadstring(readfile("Runaways_AutoGold_Fixed.lua"))()
    end
end)
]]
    local ok, err = pcall(q, payload)
    if ok then
        print("[巫script] 已登记跳服重载（受关闭标记约束）")
    else
        warn("[巫script] queue 失败:", err)
    end
    return ok
end

local function bindRemote()
    if RemoteEvent then return true end
    pcall(function()
        local fc = ReplicatedStorage:FindFirstChild("FlowClient") or ReplicatedStorage:WaitForChild("FlowClient", 12)
        local runner = fc and (fc:FindFirstChild("ClientRunner") or fc:WaitForChild("ClientRunner", 8))
        RemoteEvent = runner and (runner:FindFirstChild("Event") or runner:WaitForChild("Event", 8))
    end)
    return RemoteEvent ~= nil
end

local function fireReplay()
    if not RemoteEvent then bindRemote() end
    if RemoteEvent then
        pcall(function() RemoteEvent:FireServer("GameManager", "Replay") end)
    end
end

local function runVictory()
    if isVictoryRunning or not isRunning then return end
    isVictoryRunning = true
    task.spawn(function()
        local ok, err = pcall(function()
            local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local hrp = character:WaitForChild("HumanoidRootPart", 10)
            local humanoid = character:WaitForChild("Humanoid", 10)
            if not hrp or not humanoid then return end

            local function tp(cf)
                if not isRunning then return end
                hrp.Anchored = true
                character:PivotTo(cf)
                task.wait(0.35)
                hrp.Anchored = false
            end

            local waitTp = 6 - (tick() - SERVER_ENTRY_TIME)
            if waitTp > 0 then task.wait(waitTp) end
            if not isRunning then return end

            tp(CFrame.new(1780, 1940, 83615) * CFrame.new(0, 5, 0))
            if not isRunning then return end

            local map = workspace:FindFirstChild("Map")
            local buildings = map and map:FindFirstChild("Buildings")
            local customsFinal = buildings and buildings:FindFirstChild("CustomsFinal")
            if customsFinal then
                local pivot = customsFinal:IsA("Model") and customsFinal:GetPivot() or customsFinal.CFrame
                tp(pivot * CFrame.new(0, 5, 20))
            end
            if not isRunning then return end

            local waitDie = 7 - (tick() - SERVER_ENTRY_TIME)
            if waitDie > 0 then task.wait(waitDie) end
            if not isRunning then return end

            if humanoid and humanoid.Health > 0 then
                humanoid.Health = 0
            else
                pcall(function() character:BreakJoints() end)
            end

            task.wait(1)
            local t0 = tick()
            while isRunning and isVictoryRunning and tick() - t0 < 25 do
                fireReplay()
                task.wait(0.8)
            end
        end)
        if not ok then warn("[巫script] 流程出错:", err) end
        isVictoryRunning = false
    end)
end

local function findReplayBtn()
    local pg = LocalPlayer:FindFirstChild("PlayerGui")
    if not pg then return nil end
    local endFrame = pg:FindFirstChild("EndFrame")
    if endFrame then
        local f = endFrame:FindFirstChild("Frame")
        local opt = f and f:FindFirstChild("Options")
        local r = opt and opt:FindFirstChild("Replay")
        if r then return r end
    end
    for _, d in ipairs(pg:GetDescendants()) do
        if d.Name == "Replay" and d:IsA("GuiButton") then return d end
    end
    return nil
end

local function startFarm()
    if isRunning then return end
    setUserDisabled(false)
    isRunning = true
    queueReload()
    bindRemote()

    task.spawn(function()
        while isRunning do
            if isUserDisabled() then break end
            queueReload()
            if not isVictoryRunning then runVictory() end
            local t0 = tick()
            while isRunning and isVictoryRunning and tick() - t0 < 30 do task.wait(0.5) end
            if isRunning then fireReplay() end
            task.wait(1.5)
        end
    end)

    task.spawn(function()
        while isRunning do
            local b = findReplayBtn()
            if b and b.Visible and b.AbsoluteSize.X > 0 then
                fireReplay()
                task.wait(1.2)
            end
            task.wait(0.4)
        end
    end)
    print("[巫script] 已开启：6s传送 / 7s自杀")
end

local function stopFarm()
    isRunning = false
    isVictoryRunning = false
    setUserDisabled(true)
    print("[巫script] 已关闭，跳服将不再自动加载")
end

local function getUiParent()
    local ok, h = pcall(function() if gethui then return gethui() end end)
    if ok and h then return h end
    ok, h = pcall(function() return game:GetService("CoreGui") end)
    if ok and h then return h end
    return LocalPlayer:WaitForChild("PlayerGui")
end

pcall(function()
    local parent = getUiParent()
    local old = parent:FindFirstChild("RunawaysAutoGoldUI")
    if old then old:Destroy() end
end)

local gui = Instance.new("ScreenGui")
gui.Name = "RunawaysAutoGoldUI"
gui.ResetOnSpawn = false
gui.IgnoreGuiInset = true
gui.DisplayOrder = 9999
gui.Parent = getUiParent()

local frame = Instance.new("Frame")
frame.Size = UDim2.fromOffset(280, 130)
frame.Position = UDim2.new(0.5, -140, 0.2, 0)
frame.BackgroundColor3 = Color3.fromRGB(28, 28, 36)
frame.Active = true
frame.Draggable = true
frame.Parent = gui
Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -16, 0, 28)
title.Position = UDim2.fromOffset(8, 8)
title.BackgroundTransparency = 1
title.Text = "巫script | 离家同盟 · 刷金币"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.Font = Enum.Font.GothamBold
title.TextSize = 15
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

local sub = Instance.new("TextLabel")
sub.Size = UDim2.new(1, -16, 0, 18)
sub.Position = UDim2.fromOffset(8, 36)
sub.BackgroundTransparency = 1
sub.Text = "状态：—"
sub.TextColor3 = Color3.fromRGB(160, 160, 170)
sub.Font = Enum.Font.Gotham
sub.TextSize = 12
sub.TextXAlignment = Enum.TextXAlignment.Left
sub.Parent = frame

local btn = Instance.new("TextButton")
btn.Size = UDim2.new(1, -24, 0, 44)
btn.Position = UDim2.fromOffset(12, 64)
btn.BackgroundColor3 = Color3.fromRGB(70, 55, 30)
btn.Text = "开启自动刷金币"
btn.TextColor3 = Color3.fromRGB(255, 230, 180)
btn.Font = Enum.Font.GothamBold
btn.TextSize = 16
btn.Parent = frame
Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 8)

local function refreshBtn()
    if isRunning then
        btn.Text = "关闭自动刷金币"
        btn.BackgroundColor3 = Color3.fromRGB(160, 90, 30)
        btn.TextColor3 = Color3.fromRGB(255, 255, 255)
        sub.Text = "状态：运行中（6s传送 / 7s自杀）"
    else
        btn.Text = "开启自动刷金币"
        btn.BackgroundColor3 = Color3.fromRGB(70, 55, 30)
        btn.TextColor3 = Color3.fromRGB(255, 230, 180)
        sub.Text = "状态：已停止（不会自动加载）"
    end
end

btn.MouseButton1Click:Connect(function()
    if isRunning then
        stopFarm()
    else
        SERVER_ENTRY_TIME = tick()
        startFarm()
    end
    refreshBtn()
end)

task.spawn(function()
    while task.wait(2) do
        if gui and not gui.Parent then gui.Parent = getUiParent() end
    end
end)

task.spawn(function()
    task.wait(1)
    updateLocalCache()
    bindRemote()
    if isUserDisabled() then
        refreshBtn()
        print("[巫script] 关闭标记存在，不自动开刷")
        return
    end
    startFarm()
    refreshBtn()
end)
