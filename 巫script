local Spectrum = loadstring(game:HttpGet("https://raw.githubusercontent.com/alperalp808/Spectrum-UI-Library/refs/heads/main/Spectrum%20Source"))()

-- 1. 窗口创建与 Key 系统
local Window = Spectrum:AddWindow({
    Name = "巫 script",
    Theme = "Aqua",
    ToggleKeybind = Enum.KeyCode.RightControl,
    Size = UDim2.new(0, 500, 0, 340),
    Notifications = true,
    ToggleButton = true,
    
    KeySystem = {
        Enabled = true,
        Users = {
            { User = "admin", Pass = "1234" },
            { User = "testuser", Pass = "abcd" }
        }
    }
})

-- 欢迎通知
Window:Notify({
    Title = "欢迎！",
    Content = "巫 script 已准备就绪。",
    Duration = 3
})

-- 反挂机
local vu = game:GetService("VirtualUser")
game:GetService("Players").LocalPlayer.Idled:Connect(function()
    vu:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    task.wait(1)
    vu:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)

-------------------------------------------------
-- 标签页 1：通用功能
-------------------------------------------------
local MainTab = Window:AddTab("通用功能")
local SpeedSection = MainTab:AddSection("角色属性")

-- 所有 Slider 的 Notifications 全部设为 false，防止加载失败
SpeedSection:AddSlider({
    Name = "行走速度 (WalkSpeed)",
    Min = 16,
    Max = 250,
    Default = 16,
    Increment = 1,
    Notifications = false, 
    Callback = function(value)
        pcall(function()
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
        end)
    end
})

SpeedSection:AddSlider({
    Name = "跳跃高度 (JumpPower)",
    Min = 50,
    Max = 300,
    Default = 50,
    Increment = 1,
    Notifications = false,
    Callback = function(value)
        pcall(function()
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = value
        end)
    end
})

SpeedSection:AddSlider({
    Name = "重力 (Gravity)",
    Min = 0,
    Max = 500,
    Default = 196.2,
    Increment = 1,
    Notifications = false,
    Callback = function(value)
        workspace.Gravity = value
    end
})

SpeedSection:AddSlider({
    Name = "视野 (FOV)",
    Min = 30,
    Max = 120,
    Default = 70,
    Increment = 1,
    Notifications = false,
    Callback = function(value)
        workspace.CurrentCamera.FieldOfView = value
    end
})

local ResetSection = MainTab:AddSection("快捷操作")

ResetSection:AddButton({
    Name = "恢复默认属性",
    Callback = function()
        pcall(function()
            local char = game.Players.LocalPlayer.Character
            char.Humanoid.WalkSpeed = 16
            char.Humanoid.JumpPower = 50
            workspace.Gravity = 196.2
            workspace.CurrentCamera.FieldOfView = 70
        end)
        Window:Notify({
            Title = "已恢复",
            Content = "所有属性已重置为默认值",
            Duration = 2
        })
    end
})

-------------------------------------------------
-- 标签页 2：设置
-------------------------------------------------
local SettingTab = Window:AddTab("设置")
local InfoSection = SettingTab:AddSection("玩家信息")

InfoSection:AddButton({
    Name = "查看用户名",
    Callback = function()
        Window:Notify({
            Title = "用户名",
            Content = game.Players.LocalPlayer.Name,
            Duration = 3
        })
    end
})

InfoSection:AddButton({
    Name = "查看注入器",
    Callback = function()
        local executor = "未知"
        pcall(function()
            -- 增加保护，防止 identifyexecutor 导致崩溃
            executor = identifyexecutor() or "未知"
        end)
        Window:Notify({
            Title = "注入器",
            Content = executor,
            Duration = 3
        })
    end
})

InfoSection:AddButton({
    Name = "查看服务器ID",
    Callback = function()
        Window:Notify({
            Title = "GameId",
            Content = tostring(game.GameId),
            Duration = 3
        })
    end
})

local UtilitySection = SettingTab:AddSection("服务器操作")

UtilitySection:AddButton({
    Name = "重新加入服务器",
    Callback = function()
        game:GetService("TeleportService"):TeleportToPlaceInstance(
            game.PlaceId,
            game.JobId,
            game.Players.LocalPlayer
        )
    end
})

UtilitySection:AddButton({
    Name = "离开服务器",
    Callback = function()
        game:Shutdown()
    end
})

-------------------------------------------------
-- 标签页 3：通用1
-------------------------------------------------
local Tab1 = Window:AddTab("通用1")
local VisualSection = Tab1:AddSection("视觉与移动")

local NightVision = false
VisualSection:AddButton({
    Name = "夜视 (点击开关)",
    Callback = function()
        NightVision = not NightVision
        game.Lighting.Ambient = NightVision and Color3.new(1, 1, 1) or Color3.new(0, 0, 0)
        Window:Notify({Title = "夜视", Content = NightVision and "已开启" or "已关闭", Duration = 2})
    end
})

local NoclipEnabled = false
local NoclipConnection
VisualSection:AddButton({
    Name = "穿墙 (跳跃即关闭)",
    Callback = function()
        NoclipEnabled = not NoclipEnabled
        if NoclipEnabled then
            NoclipConnection = game:GetService("RunService").Stepped:Connect(function()
                pcall(function()
                    for _, part in pairs(game.Players.LocalPlayer.Character:GetDescendants()) do
                        if part:IsA("BasePart") then part.CanCollide = false end
                    end
                end)
            end)
            Window:Notify({Title = "穿墙", Content = "已开启（跳跃即可关闭）", Duration = 2})
        else
            if NoclipConnection then NoclipConnection:Disconnect() end
            Window:Notify({Title = "穿墙", Content = "已关闭", Duration = 2})
        end
    end
})

local InfiniteJumpEnabled = false
VisualSection:AddButton({
    Name = "无限跳 (点击开关)",
    Callback = function()
        InfiniteJumpEnabled = not InfiniteJumpEnabled
        Window:Notify({
            Title = "无限跳",
            Content = InfiniteJumpEnabled and "已开启" or "已关闭",
            Duration = 2
        })
    end
})

game:GetService("UserInputService").JumpRequest:Connect(function()
    if InfiniteJumpEnabled then
        pcall(function()
            game.Players.LocalPlayer.Character:FindFirstChildOfClass("Humanoid"):ChangeState(Enum.HumanoidStateType.Jumping)
        end)
    end
    if NoclipEnabled then
        NoclipEnabled = false
        if NoclipConnection then NoclipConnection:Disconnect() end
        Window:Notify({Title = "穿墙", Content = "已通过跳跃关闭", Duration = 2})
    end
end)

local ToolSection = Tab1:AddSection("工具")

ToolSection:AddButton({
    Name = "点击传送工具",
    Callback = function()
        local mouse = game.Players.LocalPlayer:GetMouse()
        local tool = Instance.new("Tool")
        tool.RequiresHandle = false
        tool.Name = "[巫] 传送工具"
        tool.Activated:Connect(function()
            local pos = mouse.Hit + Vector3.new(0, 2.5, 0)
            pcall(function()
                game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(pos.X, pos.Y, pos.Z)
            end)
        end)
        tool.Parent = game.Players.LocalPlayer.Backpack
        Window:Notify({Title = "提示", Content = "传送工具已添加到背包", Duration = 2})
    end
})

-------------------------------------------------
-- 标签页 4：通用2
-------------------------------------------------
local Tab2 = Window:AddTab("范围设置")
local HitboxSection = Tab2:AddSection("范围调整")

local HitboxSize = 0
local HitboxEnabled = false

game:GetService("RunService").RenderStepped:Connect(function()
    if HitboxEnabled then
        for _, v in pairs(game:GetService("Players"):GetPlayers()) do
            if v ~= game.Players.LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                pcall(function()
                    local root = v.Character.HumanoidRootPart
                    root.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
                    root.Transparency = 0.7
                    root.BrickColor = BrickColor.new("Really red")
                    root.Material = Enum.Material.Neon
                    root.CanCollide = false
                end)
            end
        end
    end
end)

local function SetHitbox(size)
    HitboxSize = size
    HitboxEnabled = true
    Window:Notify({Title = "范围", Content = "已设置为 " .. tostring(size), Duration = 2})
end

HitboxSection:AddButton({Name = "0范围", Callback = function() SetHitbox(0) end})
HitboxSection:AddButton({Name = "普通范围", Callback = function() SetHitbox(30) end})
HitboxSection:AddButton({Name = "中等范围", Callback = function() SetHitbox(100) end})
HitboxSection:AddButton({Name = "全图范围", Callback = function() SetHitbox(500) end})
HitboxSection:AddButton({Name = "终极范围", Callback = function() SetHitbox(2500) end})
HitboxSection:AddButton({Name = "关闭范围", Callback = function() 
    HitboxEnabled = false 
    Window:Notify({Title = "范围", Content = "已关闭", Duration = 2})
end})
